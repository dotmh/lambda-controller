const {expect} = require('chai');
const {Controller} = require('./helper');
const {event} = require('./mocks');

describe("LambdaController", function () {
    
    describe('Constractor', function () {
        it("should construct the class and load the event");
    });

});